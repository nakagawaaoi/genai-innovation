# デプロイ運用メモ（GitHub Pages）

このサイトは **GitHub Pages** で公開している。`main` ブランチに push すると、GitHubが自動で「ビルド → 公開」を行う。この自動処理は **GitHub Actions** という仕組みの上で動いている。

---

## 症状：こんなメールが届く

```
[nakagawaaoi/genai-innovation] Run failed at startup: pages build and deployment - main (xxxxxxx)
pages build and deployment: No jobs were run
```

これは「**サイトを公開する作業が、始まる前の段階で失敗した**」という意味。作業員が現場に来る前に、そもそも招集が失敗したイメージ。
**コードが壊れているわけではない。**

### 実害
デプロイが失敗している間、**push した変更はサイトに反映されない**。ローカルとGitHubのコードは最新でも、公開されているページだけ古いまま、という状態になる。気づきにくいので注意。

---

## 対処手順（この順番で）

### STEP 1 ── まずGitHubの障害を疑う（最重要）

**https://www.githubstatus.com/ を開く。**

ここで **Actions** の欄を見る。

| 表示 | 意味 | やること |
|---|---|---|
| **Major Outage / Degraded** | GitHub側の障害。**こちらに原因はない** | **何もしない。復旧を待つ。** push し直しても無駄 |
| **Operational** | GitHubは正常。こちら側の問題の可能性 | STEP 2 へ |

> 📌 **2026年7月25日の事例**：まさにこのメールが2回続けて届いた。原因を調べたら **GitHub Actions が Major Outage（大規模障害）** の最中だった。pushの回数や書き方は一切関係なかった。
> **教訓：原因を推測で決めつけず、必ず最初にステータスページを確認する。**

### STEP 2 ── 少し待ってから、再デプロイを1回だけ

GitHubが正常なら、**5〜10分ほど待ってから**空コミットで再デプロイを促す。

```bash
git commit --allow-empty -m "Retry deployment"
git push origin main
```

（`--allow-empty` は「中身の変更なしのコミット」。「もう一回デプロイして」という合図を送るためだけのもの。安全）

### STEP 3 ── 反映されたか確認する

```bash
curl -s "https://nakagawaaoi.github.io/genai-innovation/?t=$(date +%s)" | grep "確認したい文言"
```

`?t=...` はブラウザ／CDNのキャッシュを避けるためのおまじない。

### ⚠️ やってはいけないこと

- **失敗するたびに何度も push し直す**（デプロイ待ち行列を増やすだけで、状況が悪化する）
- 原因を確認せずに「自分のやり方が悪い」と決めつけて設定をいじる

---

## 予防：pushをまとめる

### 原則：**コミットはこまめに、pushはまとめて**

| 操作 | 頻度 | 理由 |
|---|---|---|
| `git commit` | **こまめでOK** | ローカルの操作。外部に一切影響しない。履歴が細かく残る方が後で戻しやすい |
| `git push` | **作業のまとまりごとに1回** | pushのたびにデプロイが1回走るため |

**1回のpushで複数のコミットをまとめて送れる。** pushはコミットを1個ずつ送る操作ではないので、10個コミットを溜めてから1回pushしても、全部ちゃんと反映される。

### 具体的な運用

✅ **push してよい**
- ユーザーの依頼ひとつぶんの作業が終わったとき
- 「反映して」「公開して」と言われたとき
- 作業を中断・終了するとき

❌ **push しない**
- ファイルを1つ直すたび
- 同じ依頼の中で試行錯誤している途中（文言を書き換えて、また書き換えて…の各段階）
- **1時間に5回を超えるpush**（GitHub Pagesには1時間あたり10ビルド前後のソフト上限がある）

### なぜ「3回に1回」ではなく「まとまりごと」なのか

「N回に1回push」という機械的なルールだと、「今まさに反映したい変更」が溜まったまま止まってしまうことがある。**作業の区切り**を基準にした方が、無駄なデプロイを減らしつつ「見たいときには反映されている」状態を保てる。

---

## 参考：確認に使うURL

- サイト本体：https://nakagawaaoi.github.io/genai-innovation/
- GitHubの稼働状況：https://www.githubstatus.com/
- Actionsの実行履歴：https://github.com/nakagawaaoi/genai-innovation/actions
