# 独自ドメイン genai-innov.com の構成メモ

> **2026-09-07 設定完了。** サイトは `https://genai-innov.com/` で公開中。
> このファイルは**公開リポジトリの中**にある。お名前.comのIDやパスワードなど、
> 人に見せてはいけない情報はここに書かないこと（→ 置き場所は末尾に記載）。

---

## いまの構成（どこで何が動いているか）

```
   お名前.com（ドメインの持ち主・DNSの管理）
        │  「genai-innov.com はこのサーバーにあります」と案内する
        ▼
   GitHub Pages（サイトの中身を置いている場所）
        │  main ブランチに push すると自動で公開される
        ▼
   https://genai-innov.com/
```

| 役割 | どこ | 備考 |
|---|---|---|
| ドメインの登録・DNS | お名前.com | 年1,408円（2年目以降）・自動更新ON・期限 2027/09/07 |
| サイトの中身 | GitHub リポジトリ `nakagawaaoi/genai-innovation` | **公開リポジトリ** |
| 公開（ホスティング） | GitHub Pages | 無料。**レンタルサーバーは契約していない** |
| SSL証明書（鍵マーク） | Let's Encrypt（GitHubが自動発行・自動更新） | 費用ゼロ・手作業ゼロ |

---

## DNSレコード（お名前.comに設定済み）

**ネームサーバーは `01.dnsv.jp` 〜 `04.dnsv.jp`**（お名前.comのDNSレコード設定を使うための指定）。

| ホスト名 | タイプ | 値 |
|---|---|---|
| genai-innov.com | A | 185.199.108.153 |
| genai-innov.com | A | 185.199.109.153 |
| genai-innov.com | A | 185.199.110.153 |
| genai-innov.com | A | 185.199.111.153 |
| genai-innov.com | AAAA | 2606:50c0:8000::153 |
| genai-innov.com | AAAA | 2606:50c0:8001::153 |
| genai-innov.com | AAAA | 2606:50c0:8002::153 |
| genai-innov.com | AAAA | 2606:50c0:8003::153 |
| www.genai-innov.com | CNAME | nakagawaaoi.github.io |

※ この値は**GitHubが公式に案内している公開情報**なので、ここに書いても問題ない。
　 出典: https://docs.github.com/pages/configuring-a-custom-domain-for-your-github-pages-site

### お名前.comで設定するときの注意（今回ハマった点）

- **CNAMEの値の末尾に `.` を付けない。**「末尾の「.」ドットは自動的に設定されます」とエラーになる。
- **「DNSレコード設定用ネームサーバー変更確認」のチェックを必ず入れる。** 入れないとレコードを登録しても反映されない。
- **ゾーンファイルのインポートは書式が通らなかった。** 1件ずつ手入力する方が速い。
- 途中で有料オプションの勧誘が2回出る（ドメインプロテクション 年1,078円／別ドメイン1年無料）。**どちらも「設定しない」「登録しない」でよい。**

---

## GitHub側の設定

- リポジトリ直下に **`CNAME`** というファイルを置く（中身は `genai-innov.com` の1行だけ）。これだけでカスタムドメインとして認識される。
- Settings → Pages で **Enforce HTTPS が ON**。
- 旧URL `https://nakagawaaoi.github.io/genai-innovation/` は**新ドメインへ自動転送（301）される**。名刺やXに載せた旧URLは切れない。

### 順番を守ること
**① DNS設定 → ② CNAMEファイルをpush → ③ HTTPS強制** の順。
GitHubを先にやるとDNSが効くまでサイトが見えなくなる。

---

## SEO（検索で見つけてもらうための設定）

2026-09-07 に以下を入れた。

| ファイル | 中身 |
|---|---|
| `index.html` | title・descriptionを「新潟＋生成AI導入支援／AI研修／AI顧問」で当たる形に。canonical・OGPを新ドメインへ。構造化データを `ProfessionalService` に拡張（所在地・商圏・設立日・7サービスと価格） |
| `services.html` | canonical・OGPを新設。パンくず（BreadcrumbList）とよくいただくご相談（FAQPage）の構造化データを追加 |
| `robots.txt` | 全ページ巡回OK＋サイトマップの場所を明示 |
| `sitemap.xml` | トップと事業内容ページの2件。ページを増やしたら**ここに追記する** |

### Google Search Console（2026-09-07 登録完了）

- **登録アカウントは会社の Google アカウント**（`genai.innov.info@gmail.com`）。個人アカウントではない
- プロパティの種類は **「ドメイン」**。`genai-innov.com` の www 有無・http/https・将来のサブドメインを全部まとめて見られる
- **所有権の確認方法は DNS の TXT レコード。**お名前.comに次の1行を追加してある:

  | ホスト名 | タイプ | 値 |
  |---|---|---|
  | genai-innov.com | TXT | `google-site-verification=p0lPNEJYkL7EsS7rhi_WeHdx2ys5M7ijpIFDEPylxbA` |

  **このTXTレコードは消さないこと。**消すと所有権の確認が外れ、Search Consoleが使えなくなる
- サイトマップ `https://genai-innov.com/sitemap.xml` を送信済み
- トップページと `services.html` の**インデックス登録をリクエスト済み**（優先クロールキューに追加された）

#### 2026-09-07 時点で残っている表示
サイトマップのステータスが **「取得できませんでした」**のまま。
ただし**サイト側に問題はない**ことを確認済み（Googlebot の User-Agent でも HTTP 200、robots.txt も正常、XMLも妥当）。
**ドメインを作った当日のため、Google側がまだ新ドメインを認識していないだけ**と考えられる。
→ **1〜3日後にもう一度サイトマップの画面を見て、「成功しました」に変わっているか確認する。**
　 変わっていなければ、同じ画面から再送信すればよい。

### まだ手をつけていないこと（効果が大きい順）
1. Googleビジネスプロフィール（「新潟 AI コンサル」などの地図検索で出るようになる）
2. 実績・お客様の声・事例ページ（検索に強いのは、結局こういう中身のあるページ）
3. 検索での実際の表示状況は、登録から数日〜2週間ほどで Search Console の「検索パフォーマンス」に出はじめる

---

## 秘密の情報の置き場所

**お名前ID・パスワード・管理画面のログイン情報はこのファイルには書かない。**
お名前ID などは非公開リポジトリ側の
`02_経理/資料/会社基本情報.md`（GitHub上で private）にまとめてある。

パスワードそのものはどのファイルにも書かず、Macのキーチェーン／ブラウザのパスワード管理に預ける。
